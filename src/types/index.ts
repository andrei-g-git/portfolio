import {
    ExperienceSchema,
    AboutSchema,
    SkillSchema
} from "./schemas";

import {
    LinkButtonProps,
    GaugeProps,
    SkillProps
} from "./props";

export type {
    ExperienceSchema, 
    AboutSchema,
    LinkButtonProps,
    GaugeProps,
    SkillSchema,
    SkillProps
}